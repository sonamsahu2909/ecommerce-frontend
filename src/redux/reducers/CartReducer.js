import {
    ADD_TO_CART,
    REMOVE_CART_ITEM,
    SAVE_SHIPPING_INFO,
  } from '../constants/CartConstants'
  
  export const CartReducer = (
    state = { cartItems: [], shippingInfo: {} },
    action,
  ) => {
    
  }